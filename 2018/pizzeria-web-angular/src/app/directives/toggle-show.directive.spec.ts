import { ToggleShowDirective } from './toggle-show.directive';

describe('ToggleShowDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleShowDirective();
    expect(directive).toBeTruthy();
  });
});
